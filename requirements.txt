# Phase 1-12 stack
pydantic>=2.7
pydantic-settings>=2.1
python-dotenv>=1.0
pyyaml>=6.0
pytest>=8.0

# Data & analysis
pandas>=2.2
numpy>=1.26
scipy>=1.13
statsmodels>=0.14
yfinance>=0.2.32  # US market data (free, stocks & options)
alpha-vantage>=2.3.1  # US market data alternative (free tier)

# Storage & AI / integrations
firebase-admin>=6.5
google-genai>=1.0
httpx>=0.27
websockets>=13.0
pnsea>=1.1
stealthkit>=1.0.3
curl-cffi>=0.10.0
# Pin this to avoid the `Invalid database id %28default%29` / `400` write failures
# seen with google-cloud-firestore / google-api-core>=2.35 on the default `(default)` database.
google-cloud-firestore==2.21.0
google-api-core<2.35.0

# Web dashboard
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
websocket-client>=1.6.4